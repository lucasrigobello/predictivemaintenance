apiVersion: v1
kind: Service
metadata:
  name: predictivemaintenance-api-service
spec:
  selector:
    app: predictivemaintenance-api
  ports:
    - protocol: "TCP"
      port: 8000
      targetPort: 8000
  type: LoadBalancer